# lyj-study「零打字测评」改造方案 v2

> 定位前提（用户 2026-09-01 补充）：**已购纸质同步练习与试卷 → 书写 / 计算训练由纸笔承担；网站只负责快速测出掌握情况。孩子不会打字，尽可能减少必须打字的题型。**
> 本方案基于对当前 master（v83t / commit 1fdf497）**三科全量题库实测**（8352 题）出具，逐条核对豆包《PC端无键盘化改造实施文档》，采纳其可取处，**否决 5 处关键设计**。

---

## 0. 一句话结论

**不是"给输入框加个虚拟键盘"，而是"把题库里 2341 道需要打字的题，批量改造成点选题"。**
改造后可做到 **必须打字的题 = 0 道（当前 2341 道，占 28.0%）**，且**不删任何一道题、不改任何考核点**。

| 指标 | 现状 | 豆包方案后 | 本方案后 |
|---|---|---|---|
| 必须打字的题 | 2341 道（28.0%） | 数学计算类仍需点键盘（≈0 打字但慢） | **0 道** |
| 中文词题单次作答 | 拼音 6–10 键 + 选字 | 4 项词卡，1 次点击 | 4 项词卡，1 次点击 |
| 英语单词题单次作答 | 5–8 键逐个字母 | 26 键字母盘，5–8 次点击 | **本单元词表 4 选 1，1 次点击** |
| 数值题单次作答 | 2–3 键 + 切输入法 | 20 键网格，点符号要找 | **分层数字键盘，2–4 次点击** |
| 覆盖端 | — | 仅 PC（js/pc.js） | **移动端 + PC 全端** |
| 干扰项来源 | — | 同套题其他答案（**质量差，见 §3.2**） | **同源同类词表（单元词语表 / PEP 教材词表 / 概念术语表）** |

---

## 1. 实测数据（8352 题全量 dump，脚本 `_kb_free_audit.js`）

### 1.1 题型 × 是否需打字

| 学科 | 题型 | 题量 | 需打字 | 占比 |
|---|---|---:|---:|---:|
| 数学 | choice | 1721 | 0 | — |
| 数学 | shape_choice | 391 | 0 | — |
| 数学 | **fill** | **732** | **732** | **25.7%**（占数学） |
| 语文 | choice | 1587 | 0 | — |
| 语文 | judge | 128 | 0 | — |
| 语文 | **fill** | **481** | **481** | **21.9%**（占语文） |
| 英语 | choice | 2184 | 0 | — |
| 英语 | **fill** | **1128** | **1128** | **34.1%**（占英语） |

> 豆包估「数学 25% / 语文 30%」——数学准，**语文实测 21.9%，而英语 34.1% 是三科最高，豆包完全没量化**。

### 1.2 需打字题的「答案形态」分布（决定改造路径）

**数学 732 道 fill**

| 形态 | 数量 | 占比 | 例 |
|---|---:|---:|---|
| INT 纯整数 | 625 | **85.4%** | `3+（　）=4` → 1；`比5小1的数是几` → 4 |
| CN_XS 概念词(≤3字) | 65 | 8.9% | `它是（ ）角` → 钝；`这条线没有端点…它是（ ）` → 直线 |
| MULTI 多空 | 26 | 3.6% | `角是由一个（ ）和两条（ ）组成的` → 顶点,边 |
| MIX_S 带符号短串 | 12 | 1.6% | `7.5cm` 类 |
| DECIMAL / FRACTION | 4 | 0.6% | `1.5×5` → 7.5；`1/5×4` → 4/5 |

**语文 481 道 fill**

| 形态 | 数量 | 占比 | 例 |
|---|---:|---:|---|
| **看拼音写词语** | **384** | **79.8%** | `guān cháo` → 观潮、`kuān kuò` → 宽阔 |
| **古诗文默写** | **69** | **14.3%** | `一道残阳铺水中，____` → 半江瑟瑟半江红 |
| 其他 | 28 | 5.9% | 笔画数（3，答案为数字）、拼音 `lǚ`、关联词、开放拟标题（1） |

**英语 1128 道 fill**

| 形态 | 数量 | 占比 | 例 |
|---|---:|---:|---|
| EN_WORD 单词 | 923 | **81.8%** | `根据提示写出英文单词：名字（首字母 n）` → name |
| EN_SENT 句子 | 181 | 16.0% | `连词成句：Sarah. / My / name's` → My name's Sarah. |
| MULTI / OTHER | 24 | 2.2% | 同上（含标点被判为多空） |

### 1.3 两个决定性发现

**发现 A：语文 fill 近 80% 是「看拼音写词语」，这是最不该在网站上"写"的题。**
这类题考的是**字形书写**，而用户已明确"书写训练由纸质承担"。让不会打字的孩子做这类题，是**拼音 + 选字双重障碍**（输入 `guanchao` 再从候选里找"观潮"）。它是当前孩子卡住的头号原因。

**发现 B：英语单词题的干扰项天然存在且高质量——PEP 教材库每个 unit 自带完整 `words[]` 词表。**
`js/pep.js` 每单元结构为 `{id, no, title, words:[{w,m,ipa}], sents:[], grammar}`。答案 `name` 的干扰项可直接从同单元 `words` 抽（`hello / hi / goodbye / pencil`），**同源、同册、同类**，远优于豆包"从同套题其他答案抽"。同理，语文看拼音写词语的干扰项可从**同单元同一批词语表**抽（观潮/农历/宽阔/笼罩/昂首…全是本单元词语）。

---

## 2. 设计原则（4 条，按优先级）

1. **零打字优先**：任何题都不允许出现"必须用键盘输入"才能作答的情况。
2. **检测真实性优先于作答舒适度**：干扰项必须是**同源同类**或**典型错误**，禁止"一眼假"选项——宁可少出题，也不出能被排除法/猜中蒙对的题（掌握度失真是测评产品的致命伤）。
3. **改造在题库层，不在渲染层正则**：豆包用 `isMathCalc()` 正则判断题面来分流，中文字面千变万化，误判率不可控（v83r 刚吃过判断逻辑误伤的亏）。本方案在**题目生成时**就标注 `input` 元信息，渲染层只按元信息渲染。
4. **移动端优先**：孩子实际用手机/平板作答，PC 是家长辅导端。移动端系统软键盘对不会拼音的孩子同样是障碍，**必须先改移动端**。

---

## 3. 与豆包方案的 5 处分歧（逐条给理由）

### 3.1 否决「只改 js/pc.js」→ 改为「四端锚点全改」

豆包全部改动落在 PC 渲染层。实测锚点有 **4 处渲染 + 4 处取值**：

| 端 | 渲染锚点 | 取值锚点 |
|---|---|---|
| 移动端数学 | `js/math.js:12661`（多空）/ `12667`（单空 `#answerInput`） | `12680`、`12710`、`12830` |
| 移动端语文 | `js/chinese.js:3506`（`#cnFillInput`） | `CN.submit` |
| 移动端英语 | `js/engPep.js:564`（`#pepFill`） | `submitFill` |
| PC | `js/pc.js:993`（多空）/ `998`（单空 `#pcFill`） | `1071`、`1076` |

**必须抽出一个共用输入组件**（`js/inputKit.js`），四端引用，否则会出现 PC 能点、手机不能点的分裂。

### 3.2 否决「干扰项从同套题其他答案抽取」→ 改为「同源同类候选池」

豆包 `getFillCands()` 从当前这套题的其他题答案里抽干扰项。后果：
- 语文题「看拼音写词语：guān cháo」的干扰项可能抽到 `12`（笔画数题的答案）或 `半江瑟瑟半江红`（默写题答案）——**一眼假，排除法秒过，掌握度直接失真**；
- 更糟的是干扰项可能是**同卷另一题的正确答案**，孩子选错后被判错，却记住了这个"错词"，形成错误强化。

**改为三级候选池（优先级从高到低）**：
1. **单元词表**：语文 = 同单元「看拼音写词语」答案集（本单元词语表）；英语 = `PEP.books[].units[].words[].w`；
2. **概念术语表**：数学概念类用固定术语组（锐角/直角/钝角/平角/周角；线段/射线/直线；顶点/边/张开/长短…）；
3. **典型错误生成**（仅数值题）：如分数加减法给"未通分的结果"、小数乘法给"小数点位置错的结果"。

> 这是对豆包"改选择会失真"论断的**正面破解**：失真的根源不是"有选项"，而是"选项质量差"。同源同类 + 典型错误的选项，孩子**只有真会才能选对**。

### 3.3 否决「26 字母键盘拼单词」→ 改为「本单元词表 4 选 1」

英语 923 道单词题（占英语需打字的 81.8%）。豆包方案是给 a–z 26 键让孩子点字母拼单词：
- **慢**：平均单词 5–6 个字母 = 5–6 次点击，且要逐个找字母；
- **考核点错位**：题目是「根据提示写出英文单词：名字（首字母 n，共 4 个字母）」，本质是**考词义记忆**；改成 4 选 1 后，「看中文选英文」依然完整考核该词义，且 **1 次点击**完成；
- **首字母提示已泄露一半答案**，本来就不是严格的拼写考核——那就不如干脆做成词卡，把时间省下来给真正需要的地方。

**连词成句（205 题）保留词块排序，但必须乱序**：豆包写的是"词块按答案顺序展示（已是正确顺序）"——**那等于抄写，零考核价值**。必须 `shuffle(tokens)`，考的才是语序。

### 3.4 修正「数字键盘 20 键固定网格」→ 改为「分层键盘」

实测数学 fill **85.4% 答案就是纯整数**。豆包的 20 键网格（`7 8 9 ⌫ / 4 5 6 清 / 1 2 3 ÷ / 0 . - × / ( ) / +`）对大多数题是过度设计——孩子要在 20 个键里找 `/`。

**分层方案**（由题目元信息 `input.mode` 决定，不需要正则猜）：
- `mode:'int'`（默认，覆盖 85%+）：只出 **0–9 大数字键（5×2）+ ⌫ + 清空 + 确定**，键高 ≥52px。整数平均 2–3 位 → **3–4 次点击**完成；
- `mode:'num'`：加 `.`；
- `mode:'expr'`：再加 `+ - × ÷ / ( ) %`（题面含分数/小数/算式时才展开）。

### 3.5 否决「知识点归因用单元名」→ 复用现有 `detectMathConcepts()`

豆包方案 E 用"单元名"当知识点，粒度太粗（一个单元 8 个知识点混在一起，家长看到"第三单元 50%"没法定薄弱点）。

`js/core.js:345 detectMathConcepts(text)` **已存在 27 条数学规则 + 8 类语文规则**（字词句/阅读/古诗文/习作/标点/修辞/文学常识/口语交际），v76 已接入语文批语。**直接复用做归因**，零新增规则成本。

---

## 4. 改造总纲：三层架构

```
L1 题库层（批量脚本改造，主要工作量）
   ├─ 数学：fill 题打 input 元信息；概念类（77 题）改 choice + 术语表干扰项
   ├─ 语文：看拼音写词语（384）→ choice + 同单元词表干扰项
   │        古诗文默写（69）  → choice「选正确的下句」+ 同诗/易混句干扰项
   └─ 英语：单词题（923）→ choice + PEP 单元词表干扰项
            连词成句（205）→ 打 input:{mode:'tokens'}，渲染层乱序词块

L2 组卷层（过滤器，兜底）
   └─ 拦截「无 input 元信息 且 非 choice/judge/shape_choice」的题，
      宁可换一题也不让打字题进卷（如语文 1 道开放拟标题题）

L3 渲染层（统一输入组件 js/inputKit.js，四端引用）
   ├─ mode: 'int' | 'num' | 'expr'  → 分层数字键盘
   ├─ mode: 'tokens'                → 乱序词块排序
   └─ choice / shape_choice / judge → 原样渲染，不显示任何输入面板（回归项）
```

**题目元信息约定**（新增字段，向后兼容，老题无此字段走 L2 兜底）：
```js
{ type:'fill', answer:'7.5', input:{ mode:'num' } }              // 数值点键盘
{ type:'fill', answer:'My name\'s Sarah.', input:{mode:'tokens'} } // 词块排序
{ type:'choice', answer:'观潮', options:[...] }                   // 已改造为选择
```

---

## 5. 分科目改造明细

### 5.1 数学（732 道需打字 → 0）

| 类别 | 题量 | 改造 | 单次点击 |
|---|---:|---|---:|
| INT 纯整数 625 | 85.4% | `input:{mode:'int'}` → 分层数字键盘（0–9 + ⌫ + 清 + 确定） | 3–4 |
| DECIMAL/FRACTION 4 | 0.6% | `mode:'num'` / `mode:'expr'` | 4–6 |
| CN_XS 概念词 65 | 8.9% | **改 `type:'choice'`**，干扰项来自概念术语表 | 1 |
| MIX_S 12 | 1.6% | 同上（带单位数值，改 choice，单位为干扰维度） | 1 |
| MULTI 多空 26 | 3.6% | `input:{mode:'word', blanks:n}` → **逐空词卡**（复用 L1 术语表） | 2 |

**为什么计算类保留"自己算"而不改选择**（此处与豆包结论一致，理由不同）：
计算题改 4 选 1，孩子不会时可**纯猜（25% 命中）**或**代选项反推**，成绩虚高 → 掌握度报告失真 → 家长误判 → **这恰恰摧毁了本产品唯一的目标**。数字键盘只比选择多 2–3 次点击，但保证"测出来的是真水平"。
> 计算本身不在这练（纸质承担），但**"会不会算"必须在这里测出来**。

**概念类改选择为什么不失真**：`它是（ ）角` 的选项为 `锐 / 直 / 钝 / 平`——全同类，孩子不会就是不会，猜中率 25% 但**识别类知识本来就靠记**，且这类题在一套卷里只占 1–2 题，对总评影响可忽略；而它换回的是"零打字 + 1 次点击"。

### 5.2 语文（481 道需打字 → ≤1）

| 类别 | 题量 | 改造 | 干扰项来源 |
|---|---:|---|---|
| 看拼音写词语 | 384 | **脚本批量改 `mkChoice`** | 同单元同批词语表（观潮/农历/宽阔/笼罩…） |
| 古诗文默写 | 69 | **改「选择正确的下句」** | 同首诗其他句 + 同册易混名句 |
| 笔画数（答案为数字） | 3 | `input:{mode:'int'}` | — |
| 拼音题（`lǚ`） | 2 | 改 choice | 同单元易错拼音 |
| 关联词 / 其他 | 22 | 改 choice 或逐空词卡 | 关联词固定表 |
| 开放拟标题 | 1 | **L2 过滤，不入卷** | — |

**可行性已验证**：`js/chinese.js` 现有 pool 中，`mkChoice` 的干扰项设计已很成熟（如 `'《观潮》的作者是？', '赵宗成、朱明元', ['朱自清、老舍','叶圣陶、巴金','老舍、冰心']`——作家对作家）。只需写脚本把 `mkFill('看拼音写词语：X →（ ）', 'Y')` 批量替换为 `mkChoice('看拼音写词语：X →（ ）', 'Y', [同批3个])`，**384 题脚本化改造 + 批量验证，非手工**。

### 5.3 英语（1128 道需打字 → 0）

| 类别 | 题量 | 改造 | 干扰项来源 |
|---|---:|---|---|
| 单词拼写 / 补全句子 | 923 | **改「看中文选单词」4 选 1** | `PEP` 同单元 `words[].w` 随机 3 个 |
| 连词成句 | 205 | `input:{mode:'tokens'}` → **乱序词块排序** | — |

**执行位置**：`js/engPep.js` 的题型二（105–128 行 `tag:'单词拼写'`）与题型四（178–203 行 `tag:'补全句子'` / `tag:'连词成句'`）。
- 单词题：把 `{type:'fill', judge:'eng', answer:w}` 改为 `{type:'choice', judge:'eng', options:[w, d1, d2, d3]}`，干扰项从 `unit.words` 抽（同单元，词性/主题相近）；
- 连词成句：保留 fill，追加 `input:{mode:'tokens'}`，渲染层 `shuffle(tokens)` 后出词块。

---

## 6. 输入组件规格（`js/inputKit.js`，四端共用）

```js
window.InputKit = {
  // 依据 item.input.mode / item.type 决定渲染哪种面板
  render(item, currentElId) -> html,
  bind(root) -> void,               // 事件委托，不用内联 onclick
  numpad(mode),                     // 'int' | 'num' | 'expr'
  wordpad(cands),                   // 概念/多空逐空词卡
  tokens(shuffledTokens),           // 连词成句乱序词块
};
```

**交互与尺寸**（沿用项目既有规范：按钮 ≥48px、移动端 480px 居中）：
- 数字键：5 列 × 2 行，键高 **52px**，字号 20px；功能键（⌫/清空）同排；「确定」全宽 48px；
- 词卡：2 列（移动端）/ 4 列（PC），最小高度 48px，圆角胶囊；
- 词块：横向 flex-wrap 胶囊，选中后从"待选区"移入"已选区"，支持点已选区移除（**必须支持撤销**，否则点错要清空重来）；
- **多空题**：当前聚焦空高亮（金色描边），未聚焦时默认第 1 个空，上方提示"先点要填的空"；
- **判分归一化**：点选只产出规范半角字符（`0-9 . / - + × ÷`），杜绝全角/空格导致的误判（手输时代的隐性丢分项）；
- 面板**常驻显示**（不折叠），且**不遮挡题干与配图**（PC 放题干下方，移动端放题干下方、提交按钮上方）。

**不显示面板的题型（回归项）**：`choice` / `shape_choice` / `judge`。

---

## 7. 测评闭环（让"快速测出掌握情况"真正成立）

> 豆包方案 E 方向对，但粒度太粗。以下三项全部基于**已有资产**，增量很小。

### 7.1 掌握度报告（替代单一总分）

结果页在分数卡下方增加「**知识点掌握度**」区块：
- 归因用 `js/core.js detectMathConcepts()`（数学 27 规则 + 语文 8 类），而非单元名；
- 每行：`知识点名　正确/总数　进度条　正确率`，**正确率升序（最弱排最前）**，`<60%` 标红并加「薄弱」标签；
- 精确到知识点后，家长看到的是「**异分母分数加减法 1/4**」而不是「第三单元 60%」。

### 7.2 错题本按知识点聚类

错题入库时附 `concepts: detectMathConcepts(题干)`。错题本分组展示（现为流水账），组头显示该知识点累计错误次数，支持"只看最弱 3 个知识点"。

### 7.3 纸质练习联动（闭环收口）

薄弱知识点（正确率 <60%）旁出提示：
> 建议巩固：《人教版 数学 六年级上册》**分数除法** —— 用**纸质同步练习**对应单元练书写与计算（网站上点选测的是"会不会"，写与算交给纸笔）。

由 `S.grade / S.semester / 知识点→单元映射` 自动生成文案。**这条提示是这个产品定位的关键收口**：告诉家长"网站测出的薄弱点，去纸质练习第几单元补"。

### 7.4 「快速测评」入口（新增，体现"快速"）

| 模式 | 题量 | 建议时长 | 用途 |
|---|---:|---:|---|
| 快速测评 | 10 题 | 5 分钟 | 每天一次，跨单元抽题，出掌握度雷达 |
| 单元体检 | 15 题 | 8 分钟 | 学完一单元做一次 |
| 期中/期末模拟 | 24 题 | 20 分钟 | 沿用现有组卷 |

快速测评优先抽**历史错题知识点 + 未测知识点**（基于已有 `math_practice_data`），避免每次都测已掌握的。

---

## 8. 实施分期

| 期 | 内容 | 涉及文件 | 风险 |
|---|---|---|---|
| **P0** | 输入组件 `js/inputKit.js` + 数学 `mode:'int'/'num'/'expr'` 分层键盘，**四端接入** | 新建 `js/inputKit.js`；改 `js/math.js`(12661/12667/12680…)、`js/pc.js`(993/998/1071/1076)、`css/style.css`、`css/pc.css` | 低（纯新增，不改判分） |
| **P1** | 语文 384 道看拼音写词语 **脚本批量改 choice** + 69 道默写改选句 | `js/chinese.js`（脚本化）、`_cn_fill_to_choice.js`（转换脚本） | 中（批量改题库，需全量回归） |
| **P2** | 英语 923 道单词题改单元词表 4 选 1 + 205 道连词成句乱序词块 | `js/engPep.js`、`js/pep.js`（只读词表） | 中 |
| **P3** | 数学 103 道概念/多空题改 choice + 逐空词卡 | `js/math.js` | 中 |
| **P4** | 掌握度报告 + 错题聚类 + 纸质联动 + 快速测评入口 | `js/core.js`、`js/math.js`、`js/chinese.js`、`js/pc.js` | 低 |

> 每期完成后：`node --check` → 对应冒烟脚本 → 真机截图 → 升 `sw.js CACHE` → 推送。
> P1/P2/P3 属题库层改造，**必须跑 `_cap.js`（单元题量 ≥24 验收）+ `_audit_cross.js`（超纲/跨年级，须 0）+ `_cn_en_audit.js`（须 0）+ 数学全量回归**，这是项目既定铁律。

---

## 9. 验收清单

**功能**
- [ ] 数学单元练习：整数填空题出现**分层数字键盘**，点选作答判分正确
- [ ] 数学概念题（"它是（ ）角"）为 4 选 1，干扰项同类
- [ ] 语文"看拼音写词语"为 4 选 1，干扰项来自同单元词语表
- [ ] 语文默写题为「选正确的下句」
- [ ] 英语单词题为「看中文选单词」4 选 1，干扰项来自本单元 PEP 词表
- [ ] 英语连词成句：词块**乱序**，可点选、可撤销、可清空
- [ ] 多空题可逐空点选，聚焦空有高亮
- [ ] 选择 / 图形选择 / 判断题**不显示任何输入面板**
- [ ] **移动端与 PC 端表现一致**（豆包方案未覆盖移动端，此处为硬指标）

**数据**
- [ ] 全量重跑 `_kb_free_audit.js`：**需打字题数 = 0**（当前 2341）
- [ ] `_cap.js`：131 单元题量 ≥24，无 0
- [ ] `_audit_cross.js` = 0；`_cn_en_audit.js` = 0
- [ ] 数学/语文/英语既有冒烟脚本（v75 / v79~v83t）全 PASS

**回归**
- [ ] 两段式批改（成绩页 → 确认成绩 → 解析页）不受影响
- [ ] `forceFill` 降级逻辑不受影响（v83r 修复的契约必须保持）
- [ ] 错题本 / 学习统计 / 家长端同步正常
- [ ] 全端无 JS 报错

---

## 10. 附：与豆包方案逐条对照

| 豆包条目 | 本方案 | 结论 |
|---|---|---|
| 方案A 数字/符号点选键盘（20 键） | 采纳，改**分层**（默认只出 0–9） | ✅ 优化 |
| A+ 概念类改选择 | 采纳，且**在题库层改**（非渲染层正则） | ✅ 强化 |
| 2.6 `isMathCalc()` 正则分流 | **否决**（正则脆弱，改题目元信息 `input.mode`） | ❌ 替换 |
| 方案B 候选词卡（语文） | 采纳，但**干扰项改同源词表**（原方案干扰项质量差） | ⚠️ 修正 |
| 方案C 26 字母键盘拼单词 | **否决**（慢且考核点错位），改**单元词表 4 选 1** | ❌ 替换 |
| 方案C 词块按答案顺序展示 | **否决**（等于抄写），必须**乱序** | ❌ 修正 |
| 方案D 开放题口头作答 | 采纳，但**优先在组卷层过滤**，不入测评卷 | ✅ 优化 |
| 方案E 掌握度报告 | 采纳，粒度从"单元名"改为 **`detectMathConcepts()` 知识点** | ✅ 强化 |
| 方案E 纸质练习联动 | 采纳 | ✅ |
| 只改 `js/pc.js` | **否决**，必须四端（移动端 3 处 + PC 1 处）共用组件 | ❌ 替换 |
| 计算类保留键盘（不改成选择） | **采纳**，理由修正为"防猜测导致掌握度失真" | ✅ |

---

*方案生成：2026-09-01　依据 master@1fdf497（v83t）三科 8352 题全量实测（脚本 `_kb_free_audit.js`，输出 `_kb_free_out.json`）*
