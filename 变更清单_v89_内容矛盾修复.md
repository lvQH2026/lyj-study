# 变更清单 v89：修复「内容与单元名矛盾」+ 检测引擎两处静默缺陷

> 生成时间：2026-09-04 01:10:20
> 立项依据：v88 覆盖检测报告第七章单列的 4 处内容缺陷，经确认后立项修复。
> 对比基线：`_kp_v88.json`（修改前） vs `_kp_v89.json`（修改后）——**同一引擎、同一采集参数**，
> 且基线是在「引擎缺陷已修」的前提下重跑的，因此下表的变化**只反映题库内容修复**，不含引擎修复带来的增益。

## 一、本轮遵循的规范（沿用既有约定，未另立标准）

| 项 | 沿用规范 |
| --- | --- |
| 出题函数写法 | `reps` 概念题数组 + 参数化生成，`Math.random()` 分流；沿用 `g_mul_2`/`g2_time` 的既有范式 |
| 干扰项 | 一律走 `dis3(ans, ...候选)`（v87 引入），保证 3 项互异且不含正确答案；`mc()` 会自动补到 4 项 |
| 题干标点 | 沿用相邻生成器的既有写法（`?` / `？` 按原单元保持） |
| 知识点来源 | 不新增知识点条目，只让题库**出对题**去命中 `_kp_mid.js` 里已有的规则 |
| 容量铁律 | 每单元 `gen()` 不同题面 ≥24（`_cap.js` 验收） |
| 校验标准 | `node --check` → `_v89_smoke.js` → `_cap.js` → `_audit_cross.js`（须 0）→ `SCOPE=all _test_all_units.js`（须 0） |
| 诚实原则 | 内容修对了但指标没动的，照实记「持平」，不放宽规则凑数 |

## 二、内容修复（逐项列出修改位置 / 内容 / 原因）

| # | 位置 | 单元 | 修改前 | 修改后 | 修改原因 |
| --- | --- | --- | --- | --- | --- |
| 1 | `js/math.js` `g_app_average`（新建）→ 挂到 `KNOWLEDGE_BASE[4][2][11]` | 4下·11 专项·平均数应用 | 误挂 `g_app_planting`，出的全是「一条路长70米，每隔10米种一棵树，共几棵？」 | 新建 `g_app_average`，按本单元 6 个知识点出 5 类题（求平均数／逆用求总数／实际问题／比较两组／概念） | **内容与单元名完全不符**，且植树问题属 5上「数学广角」，放在 4 年级还**跨年级超纲**；该单元 6 个知识点覆盖率曾为 0% |
| 2 | `js/math.js` `g20_subtraction` | 1下·0 20以内的退位减法 | `a∈[11,18]、b∈[2,9]`，只保证不出现负数 → 约一半题目**个位够减、根本不用退位**（如 `15-4`） | 按退位的充要条件取 `b∈[a%10+1, 9]`，差恒为 2~9 | 单元名是「退位减法」，却出**不退位**的题，练不到本单元的核心算法 |
| 3 | `js/math.js` `g100_addsub_nocarry` | 1下·2 100以内的加减法（不进位） | ① 只出加法，与单元名「加减法」不符；② `a∈[20,80]、b∈[10,99-a]` 只保证和≤99，**不保证不进位**（如 `43+29`，个位 3+9=12） | 改按「十位/个位」分别取值构造，天然满足个位、十位都不进位／不退位；补上不退位减法（含两位数减一位数、两位数减两位数） | 同上：出的是**进位**题，与「不进位」完全相反；且单元名含「减法」却一道减法都没有 |
| 4 | `js/math.js` `g_mul_1` | 2上·3 表内乘法（一） | `a∈[2,5]、b∈[1,9]` → 会出 `5×7`、`4×8` 这类题 | 两因数都取 `2~6`；另补「乘法初步认识／各部分名称／乘加乘减／用乘法解决问题」四类题 | `5×7` 用的是**7 的口诀**，按人教版属「表内乘法（二）」，放在本单元即超前；同时原实现只出口诀计算，单元 5 个知识点里 4 个无题 |
| 5（附带） | `js/math.js` `g_mul_2` | 2上·4 表内乘法（二） | `a∈[6,9]` 会出 `6×6`、`6×5` | `a∈[7,9]` | 6 的口诀属表内乘法（一），与第 4 项同源，两单元不应重叠。**注：此项不在原立项的 4 项之内，是修复过程中发现的同类问题，一并处理并在此单列** |

### 2.1 `g_app_average` 题型与知识点对应

| 知识点 id | 知识点 | 对应题型 | 示例题面 |
| --- | --- | --- | --- |
| pjz01 | 求平均数（总数÷份数） | 参数化（3~5 个数，和能被个数整除） | 数据：23、18、19，平均数是？ |
| pjz02 | 平均数的逆用 | 参数化（3~6 个数） | 4个数的平均数是36，这4个数的和是？ |
| pjz03 | 平均数解决实际问题 | 4 类场景 × 参数化 | 小明5天平均每天练字23个，这些天一共练了多少个？ |
| pjz04 | 平均数在最大与最小之间 | 概念题 | 平均数一定在最大数与最小数之间，对吗？ |
| pjz05 | 用平均数比较两组数据 | 参数化（两组 3~5 个数，和必不相等） | 甲组成绩：…；乙组成绩：…，哪组成绩好（比平均数）？ |
| pjz06 | 移多补少求平均数 | 概念题 | 用"移多补少"求平均数，就是把多的部分（　） |

> 注：`g_app_planting` 的实现**保留未删**——`verify_v48.js` 回归测试按函数名字符串调用它；
> 植树问题的正式单元是 5上·数学广角（`g5_tree`）与 5上·专项（`g_tree_special`），已在函数上方加注释说明。

## 三、引擎层变更（影响检测结果，非题库内容）

| # | 位置 | 问题 | 修法 | 影响面 |
| --- | --- | --- | --- | --- |
| E1 | `_kp_cover_all.js` `collectMath()` | v88 修池模式时写成「取一次就返回」：这类 `gen()` 其实是**每次从大题库随机抽 20 道**（角度计算题库 111 种题面），只取一次等于只看 1/5 题库，覆盖率**随批次抖动**（实测同一单元在 86%~100% 之间跳，出现过 -14pt 的假回退） | 反复调用并合并去重，直到连续 15 次抽不出新题面为止 | 4 个池模式单元；数学题面采集量 9.1 万 → 16.4 万 |
| E2 | `_kp_recheck.js` | 只能查「编译失败」，查不出**能编译但匹配不到目标字符**的正则 | 新增第二道防线：扫描字符类，若含全角 `＋`/`－`/`＝` 却匹配不到对应 ASCII 符号即报警（取反类 `[^...]` 跳过，避免误报） | 一次抓出 8 处 |
| E3 | `_kp_mid.js` 等 10 处 | 字符类写成 `[+＋-－−]`，中间的 ASCII `-` 被当成**范围符**（`＋`→`－` 的码位区间），ASCII 减号 U+002D 反而不在集合内 → **所有含减号的题面一律漏判** | 批量改为 `[-+＋－−]`（连字符放最前） | 涉及 2上·3、2下·3、2下·6、3上·14、3下·6、4下·5 等单元 |

> E3 是 E2 抓出来的。它**能正常编译**，所以上一轮的 `_kp_recheck.js` 报「未发现非法正则」——
> 这类缺陷只能靠「意图检查」发现，已在 `_kp_recheck.js` 里固化为常驻体检项。

## 四、逐单元覆盖率影响（数学 131 单元全量对比）

变化的单元共 **3** 个（3 升 / 0 降）：

| 单元 | 修改前 | 修改后 | 变化 |
| --- | --- | --- | --- |
| 4下·11 专项·平均数应用 | 0/6 0% | 6/6 100% | +100pt |
| 2上·3 表内乘法（一） | 1/5 20% | 5/5 100% | +80pt |
| 1下·2 100以内的加减法（不进位） | 1/5 20% | 3/5 60% | +40pt |

### 4.1 立项的 4 个单元逐条核对

**1下·0 20以内的退位减法**：4/6 67% → 4/6 67%　题面 64 → 36　未归类 0 → 0

| 知识点 | 修改前命中 | 修改后命中 | 状态 |
| --- | --- | --- | --- |
| 十几减9（破十法/想加算减） | 8 | 8 | 保持 |
| 十几减8、7、6 | 24 | 18 | 保持 |
| 十几减5、4、3、2 | 32 | 10 | 保持 |
| 破十法与想加算减的算理 | 0 | 0 | 仍为 0（题库缺口） |
| 用减法解决问题（求剩余、求相差） | 0 | 0 | 仍为 0（题库缺口） |
| 20以内减法的口算 | 64 | 36 | 保持 |

**1下·2 100以内的加减法（不进位）**：1/5 20% → 3/5 60%　题面 2125 → 2346　未归类 0 → 0

| 知识点 | 修改前命中 | 修改后命中 | 状态 |
| --- | --- | --- | --- |
| 两位数加一位数（不进位） | 0 | 63 | **新覆盖** |
| 两位数加两位数（不进位） | 2125 | 938 | 保持 |
| 两位数减一位数、两位数（不退位） | 0 | 1345 | **新覆盖** |
| 相同数位对齐的算理 | 0 | 0 | 仍为 0（题库缺口） |
| 用加减法解决问题 | 0 | 0 | 仍为 0（题库缺口） |

**2上·3 表内乘法（一）**：1/5 20% → 5/5 100%　题面 34 → 699　未归类 0 → 0

| 知识点 | 修改前命中 | 修改后命中 | 状态 |
| --- | --- | --- | --- |
| 乘法的初步认识（求几个相同加数的和） | 0 | 25 | **新覆盖** |
| 2~6的乘法口诀 | 34 | 25 | 保持 |
| 乘法算式中各部分的名称 | 0 | 25 | **新覆盖** |
| 乘加、乘减 | 0 | 599 | **新覆盖** |
| 用乘法解决问题 | 0 | 25 | **新覆盖** |

**2上·4 表内乘法（二）**：7/7 100% → 7/7 100%　题面 35 → 29　未归类 0 → 0

| 知识点 | 修改前命中 | 修改后命中 | 状态 |
| --- | --- | --- | --- |
| 7的乘法口诀 | 11 | 10 | 保持 |
| 8的乘法口诀 | 11 | 10 | 保持 |
| 9的乘法口诀 | 12 | 11 | 保持 |
| 乘法口诀表与口诀的编制 | 30 | 24 | 保持 |
| 一个数的几倍（乘法意义的应用） | 3 | 3 | 保持 |
| 乘法算式与积的关系 | 4 | 4 | 保持 |
| 用乘法解决问题 | 1 | 1 | 保持 |

**4下·11 专项·平均数应用**：0/6 0% → 6/6 100%　题面 60 → 3670　未归类 60 → 3

| 知识点 | 修改前命中 | 修改后命中 | 状态 |
| --- | --- | --- | --- |
| 求平均数（总数÷份数） | 0 | 1631 | **新覆盖** |
| 平均数的逆用（已知平均数求总数） | 0 | 1629 | **新覆盖** |
| 平均数解决实际问题 | 0 | 798 | **新覆盖** |
| 平均数在最大与最小之间 | 0 | 1 | **新覆盖** |
| 用平均数比较两组数据 | 0 | 1237 | **新覆盖** |
| 移多补少求平均数 | 0 | 1 | **新覆盖** |

## 五、验收结果

| 验收项 | 命令 | 结果 |
| --- | --- | --- |
| 语法 | `node --check js/math.js` | 通过 |
| 内容正确性（本轮专项） | `_v89_smoke.js`（5 个生成器 × 4000 次采样） | **10 项断言全部通过** |
| 单元容量 | `_cap.js` | 容量 <24 的单元 **0 / 131** |
| 跨年级超纲 | `_audit_cross.js` | **0 种 / 0 题次** |
| 全真模拟作答 | `SCOPE=all _test_all_units.js` | 131 单元 + 203 场考试，**合计问题 0 条** |
| 既有回归 | `verify_v48.js` | 失败 31 → **30 项**，按类别对比**未引入任何新失败**（差异为参数化随机采样噪声） |
| 既有回归 | `verify_flow.js` | 失败 1 项（v81 引擎三层齐备），与基线一致，非本轮引入 |
| 规则体检 | `_kp_recheck.js` | 编译失败 **0** + 字符类漏收 **0** |
| SW 版本 | `sw.js` | `lyj-shell-v87` → **`lyj-shell-v89`**（v88 只改文档未动线上代码，故未占用） |

数学未归类题：797 → 725（题面 159703 → 164020，占比 0.50% → 0.44%）

## 六、诚实标注：未达标 / 未改动之处（不粉饰）

1. **1下·0 退位减法覆盖率持平 67%，未提升**。内容确实修对了（现在 4000 次采样全部退位，不再出现 `15-4`），
   但覆盖率指标没动——因为 `_kp_mid.js` 里 `i06 20以内减法的口算` 用的是 `{calc:{op:"-",maxA:20}}`，
   不区分是否退位，修改前的错题也被它判为命中。
   **这是规则粒度太粗，不是内容问题**；按指令第 4 条「存在歧义的地方单独列出、不擅自改动」，未改判定口径，在此列出。
2. **仍未覆盖的知识点（本轮未处理，属题库缺口，需补题才有效）**：

   - 1下·0：破十法与想加算减的算理、用减法解决问题（求剩余、求相差）
   - 1下·2：相同数位对齐的算理、用加减法解决问题

3. **语文 64 单元、英语 46 单元本轮未改动**——v88 已诊断确认为结构性歧义（语文未归类 762 条/755 种模板，多为课文内容理解；
   英语未归类 1520 条/1439 种模板，多为补全句子与中译英），改关键词规则无效，需换成结构化数据（英语题加 `tag` 字段、语文按课文建考点清单）。
4. **数学仍有 195 个知识点规则已就位但题库未出题**（占 865 的 22.5%），本轮只修了其中 4 个单元的一部分，其余需单独补题立项。

## 七、如何复跑

```bash
cd 吕泳冀学习站PWA
NODE=C:/Users/Administrator/.workbuddy/binaries/node/versions/22.22.2-2/node.exe
export NODE_PATH=C:/Users/Administrator/.workbuddy/binaries/node/workspace/node_modules

# 0）规则体检（编译失败 + 字符类漏收，均须为 0）
$NODE _kp_recheck.js

# 1）本轮内容修复的专项验收（5 个生成器 × 4000 次采样，10 项断言）
$NODE _v89_smoke.js

# 2）题库校验管线
node --check js/math.js && $NODE _cap.js && $NODE _audit_cross.js
SCOPE=all $NODE _test_all_units.js
$NODE verify_v48.js && $NODE verify_flow.js

# 3）覆盖率对比（先跑修改前基线，再跑修改后；最后一次跑的是「修改后」，报告才会是最终数据）
git checkout -- js/math.js                       # 还原到修改前
MAX_CALLS=6000 STALL=300 PAPERS=4 $NODE _kp_cover_all.js && cp _kp_cover_all.json _kp_v88.json
cp _bak/math_v89.js js/math.js                   # 恢复修改后
MAX_CALLS=6000 STALL=300 PAPERS=4 $NODE _kp_cover_all.js && cp _kp_cover_all.json _kp_v89.json
$NODE _v89_cmp.js
$NODE _kp_report_add.js                          # 追加报告第六、七章
$NODE _v89_changelog.js
```

> ⚠️ 顺序坑（v88 踩过）：跑基线会把 `_kp_cover_all.json` 变成「修改前」数据，
> 若此时拿它生成报告并推送，上线的就是旧数据。**推前必须 `head` 核对报告总览表数字。**
