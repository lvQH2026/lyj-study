# 吕泳冀学习站 · 网站改版建议 v2（整合修订版）

- **生成日期**：2026-09-01
- **评估基准**：线上 `sw.js` = `lyj-shell-v77`（已核对本地与 Pages 一致）
- **文档性质**：在《改进方案评估与规划.md》《版块重构评估与建议.md》《小升初复习方案.md》三份既有文档基础上**整合、去重、修正并新增**，本版为最新版，与旧文档冲突处以本版为准。
- **状态**：分析交付，**未改动任何代码、未推送**。
- **修订记录**
  - 2026-09-01 初版：整合前三份规划文档，输出问题诊断 + P0/P1/P2 方案 + 否决清单 + S0–S7 实施顺序。
  - 2026-09-01 修订 1：用户拍板**取消「PC 窄屏降级」**（原 P1-B4 / 阶段 S5）。已从方案、排期、SW 版本中移除，并记入第 4 节否决清单；阶段 S6/S7 顺延为 S5/S6，SW 版本相应前移。
  - 2026-09-01 修订 2：**英语 PEP 新版六上 U1 标题确认为 `Amazing places`**（家长核对课本目录页）。B4 的待确认项关闭，建库以此为准。至此 v2 文档已无待确认项。
  - 2026-09-01 修订 3：**S0 已实施完成并上线（commit `11e3c35`，SW `lyj-shell-v78`）**。实施范围在原计划基础上追加一项卫生修复（移动端 `renderHome()` 在 PC 端空引用报错），详见第 5 节 S0 行备注。
  - 2026-09-01 修订 4：**S1 已实施完成并上线（commit `d4fa8e0`，SW `lyj-shell-v79`）**。导航统一 + 年级持久化交付，实施范围在原计划基础上追加「语文考试中心」与「英语进度入口回收」两项，详见第 5 节 S1 行备注。
  - 2026-09-01 修订 5：**S2 已实施完成并上线（commit `d64ac05`，SW `lyj-shell-v80`）**。今日任务驾驶舱首屏交付，详见第 5 节 S2 行备注。
  - 2026-09-01 修订 6：**S3 已实施完成并上线（SW `lyj-shell-v81`）**。解析讲解回填交付，错题空壳率 89.2% → 0%，详见第 5 节 S3 行备注。

---

## 0. 前置约束（所有改版建议的判定基准）

以下三条由用户已确认的事实推导，任何改版项若与之冲突，一律以本约束为准：

| 约束 | 事实 | 对改版的判定影响 |
|---|---|---|
| **C1 家长远程辅导** | 孩子在广西北流，家长在广东，通过网站异地辅导 | 解析讲解、统计报表、错题同步、家长看板属**刚需**而非锦上添花；任何"孩子自己看结果"的设计都要补"家长远程可见" |
| **C2 基础一般** | 孩子基础一般，走公办对口/直升 | 默认难度锁定**基础档**（v77 三档默认 `diff=1`，符合）；拓展题不作默认路径；结果页需降低挫败感、强化引导 |
| **C3 内容与北流对齐** | 数学人教版 ✅ / 语文统编版 ✅ / 英语人教 PEP（新旧双轨）⚠️ / 真题缺玉林北流 ⚠️ | 内容组织与文案必须为"六年级 + 广西北流"服务，1–6 年级泛化入口属无效面积 |

---

## 1. 现状速览（源码可查，非推测）

| 维度 | 现状 | 源码位置 |
|---|---|---|
| 全局导航 | 顶部 `#moduleSwitch`（数学/语文/英语）常驻 48px | `index.html:15-19` |
| 数学模块 | navbar「📐 小学数学」+ 底部 5 tab（首页/考试/错题库/统计/家长），10 个页面 | `index.html:20-325` |
| 语文模块 | navbar「📖 小学语文」+ 底部 **2** tab（首页/错题库），无考试/统计/家长 | `index.html:339-430` |
| 英语模块 | navbar「英语 · 学习中心」+ 底部 4 tab（自然拼读/国际音标/错题本/进度），**无首页/单元/考试/统计/家长** | `index.html:326-338` |
| 考试中心 | `#page-exam` **仅存在于 `#mathRoot`** | `index.html:236-279` |
| 年级选择 | `state.currentGrade` 为**纯内存变量**（初值 `null`），未持久化，重开必重选 1–6 年级 | `js/math.js:9553` |
| 视觉 token | `--primary:#3E4A63 / --gold:#B4945A / --bg:#F7F6F2`，body `max-width:480px` 居中 | `css/style.css:2-39` |
| PC 端 | 侧边栏 + 角色切换（学生/家长）+ 面包屑 + 同步 chip | `pc.html:10-45` |
| PC 窄屏 | `@media(max-width:1023px){.pc-app{display:none!important}}` 硬阻断 | `css/pc.css:84-99` |
| 解析能力 | `renderAnswerKeyPage` 已预留 `explain`/`steps` 渲染位，**数据侧未填充** | `js/pc.js:1102-1103` |

---

## 2. 当前版本的主要问题与影响

### 2.1 信息架构与导航（严重度：高）

**P-01　三科导航结构不一致，语文/英语路径残缺**
- 现象：数学 5 tab，语文 2 tab，英语 4 tab 且无首页/考试/统计。
- 影响：**直接阻断复习方案落地**。《小升初复习方案》要求的"语数英三科周测 + 统计复盘"，在 App 内语文、英语根本无法执行——语文没有考试中心，英语连考试入口都不存在。这是"助力小升初"目标最大的结构性缺口。

**P-02　年级不持久化，每次打开都要重选**
- 现象：`state.currentGrade` 存内存，重开即 `null`，首页第一个区块就是"选择年级"（1–6 年级九宫格）。
- 影响：孩子实际只用六年级，却每天重复一次无意义选择；对基础一般的孩子，这是**每日启动成本**，直接损耗坚持率。与 C3 冲突。

**P-03　首页是"功能陈列"，不是"任务入口"**
- 现象：首页顺序为 banner → 选择年级 → 快速练习 → 专项练习 → 拍图上传（占大块且带 4 行说明）→ 最近练习。
- 影响：孩子打开后不知道"今天该做什么"；低频的拍图上传占据显著面积，高频的"继续上次练习"被压到最后。

**P-04　顶部 48px 模块切换 + navbar + bottom-nav 三层导航并存**
- 现象：`#moduleSwitch` 常驻顶部，模块内部又有 navbar 与底部 tab。
- 影响：移动端垂直空间被导航吃掉约 100px，答题区可视面积压缩；层级关系（学科 vs 功能）视觉上含混。

### 2.2 页面布局与视觉风格（严重度：中高）

**P-05　emoji 泛滥，与既有"禁用卡通 emoji"规范直接冲突**【新增发现】
- 现象：`index.html` 18 处（📐📖🏠🖨✅🚀📱📋📄💾⬇⬆👨‍👩‍👦）、`js/math.js` 30 处、`parent.js` 11 处、`js/core.js` 8 处、`js/pc.js` 5 处、`js/english.js` 3 处。
- 影响：① 违背已确立的轻奢极简视觉定位；② 图标风格割裂——底部导航中"首页"用 emoji 🏠、"考试/错题库/统计"用线性 SVG、"家长"用 emoji，三套体系混排；英语 tab 干脆用汉字「拼/音/错」当图标，且"进度"tab 无图标。整体呈现"拼凑感"。

**P-06　内联样式泛滥，主题无法统一治理**【新增发现】
- 现象：`index.html` 中 63 处 `style="..."`，含整块 `#page-parent` 的结构与配色写死在行内。
- 影响：改一处主色要改 63 个地方；家长后台配色（`#e6f0ff`/`#1d4ed8`）已游离于 `--primary` 体系之外。

**P-07　部分控件未达触控与可访问性标准**【新增发现】
- 现象：`.btn-outline` `min-height:42px`（`css/style.css:414`）、另一处 `44px`（:407），低于 48px 触控标准；家长登录框 `#parentId/#parentPw` 用行内样式且**未设 font-size**（iOS 聚焦时若 <16px 会触发页面缩放）。
- 影响：孩子手指误触；家长端输入框在 iOS 上聚焦跳动。

### 2.3 内容与文案（严重度：中高）

**P-08　文案与目标用户错位**
- 现象：首页 banner「人教版 · 1-6年级 · 让学习变得有趣」；PC 侧边脚「**v60** · 与手机端数据互通」（实际 v77）；语文模块注释"4-6年级 · 按知识板块组织"。
- 影响：① 版本号滞后 17 个版本，家长看到会质疑维护状态；② "1-6年级"与"锁定六年级冲刺"的目标叙事冲突；③ 英语完全无教材同步内容（仅音标拼读），与北流 PEP 教材脱节，三科中唯一无法支撑复习。

**P-09　解析内容缺失（数据侧）**
- 现象：PC 与移动端解析页的**渲染层已就绪**（`renderAnswerKeyPage` 预留 `explain`/`steps`），但数学题生成器未填充这些字段。
- 影响：错在"数据不在渲染"，对家长远程辅导是致命的——孩子错了，身边没人讲，App 也讲不出过程。这是 C1 的核心痛点。

**P-10　真题无本地卷**
- 现象：`js/gx_real.js` 六下仅覆盖南宁/防城港/河池/贺州（2024），无玉林/北流。
- 影响：与 C3"地区对齐"冲突，冲刺阶段缺少本地考情素材。

### 2.4 交互与移动端响应式（严重度：中）

**P-11　移动端禁止缩放**【新增发现】
- 现象：`index.html:5` 为 `maximum-scale=1.0, user-scalable=no`；`pc.html:5` 无此限制，两端不一致。
- 影响：违反无障碍基本要求（WCAG 1.4.4 允许缩放到 200%）；孩子看不清小字图表时无法放大，对基础一般的孩子尤其不友好。

**P-12　PC 端窄屏硬阻断**　🚫 **已决定不修（用户 2026-09-01 拍板取消）**
- 现象：`<1024px` 直接整页替换为"请在电脑端打开"，无任何降级。
- 影响：家长用平板或分屏时完全不可用。
- **处理决定**：维持现状，不做软降级改造。理由：孩子主用手机 PWA（功能完整），PC 工作台的使用场景是家长在电脑上打开，窄屏场景极少；改动收益低于其他项。后续若有强需求再议。

**P-13　移动端在宽屏下呈 480px 窄条**【新增发现】
- 现象：`body{max-width:480px}` 固定，桌面打开时两侧大片留白。
- 影响：家长在电脑上帮孩子看移动端页面时，空间利用率低、需频繁滚动。

---

## 3. 改版方案（按优先级）

> 归属标签说明：**【沿用】**＝既有结论原样保留；**【修正】**＝对既有结论的优先级/范围调整；**【新增】**＝本版首次提出。

### P0　阻断级 —— 不修则复习方案无法落地

#### A1　三科导航统一 + 年级持久化【沿用，扩范围】
- **目标**：让语文、英语具备与数学同等的完整练习闭环；消除每日重复选年级。
- **具体做法**
  1. 统一三科底部导航为 **学 / 练 / 错 / 报 / 家**（学习、考试练习、错题库、统计报表、家长）五 tab，语文、英语补齐 `#page-exam`、`#page-stats` 页面容器。
  2. 英语放弃"拼读/音标/错题/进度"的自有 tab 体系，音标与拼读降级为**英语学科内的一个学习板块**（保留数据，改入口）。
  3. 年级选择结果写入 `localStorage`，首次进入引导选一次（默认预置六年级），此后自动恢复；首页提供"切换年级"次级入口。
- **预期效果**：语文英语可执行周测与统计（复习方案可落地）；每日启动成本从"3 次点击"降到"0 次"。

#### A2　首页改「今日任务」驾驶舱【沿用，强化】
- **目标**：打开即知道今天做什么。
- **具体做法**
  1. 首页首屏改为：倒计时（距 2027 年 6 月）+ 今日任务卡（数学 X 题 / 语文 X 题 / 英语 X 词）+ 薄弱提醒（来自错题统计）+ 继续上次练习。
  2. 年级九宫格降级为折叠区或设置项；拍图上传收进"工具"折叠区。
- **预期效果**：每日任务直达，减少决策损耗，提升坚持率。

#### A3　解析讲解数据回填（每错必讲）【沿用】
- **目标**：孩子错了，App 讲得透（C1 刚需）。
- **具体做法**
  1. 在 `js/core.js` 增加通用 `buildExplain(q)`，按题型（计算/应用/选择/填空）生成分步讲解，写入 `explain` / `steps`。
  2. 数学生成器按题型批量回填；语文沿用现有 `passage`/解析字段。
  3. 复用已就绪的渲染位（移动端 `#examAnswerKey`、PC `renderAnswerKeyPage`），**渲染层基本不动**。
- **预期效果**：错题闭环形成，家长远程无需逐题讲解。

### P1　体验与一致性（成本可控，见效快）

#### B1　视觉风格治理：emoji → 统一线性 SVG【新增】
- **目标**：落实"禁用卡通 emoji"规范，消除图标三套体系混排。
- **具体做法**
  1. 建立统一图标集（inline SVG，`viewBox="0 0 24 24"`，`stroke-width:1.8`，与现有 SVG 一致），覆盖首页/考试/错题/统计/家长/学科。
  2. 按文件批量替换：`index.html`(18) → `math.js`(30) → `parent.js`(11) → `core.js`(8) → `pc.js`(5) → `english.js`(3)。
  3. **例外保留**：`★` 星级（功能性符号，非 emoji）、结果页 `✅ 确认成绩`（语义按钮，可保留）。
- **预期效果**：视觉统一，回归轻奢极简定位；连带减少 emoji 字体渲染差异。

#### B2　内联样式回收 + 控件尺寸达标【新增】
- **目标**：主题可治理，触控达标。
- **具体做法**
  1. 将 `index.html` 63 处行内样式抽为语义 class（`.prt-*`/`.card-*` 体系已存在，沿用），家长后台配色改用 CSS 变量。
  2. `.btn-outline` 等次级按钮 `min-height` 统一到 ≥48px；家长端输入框显式 `font-size:16px`。
- **预期效果**：改一次变量全站生效；误触与 iOS 缩放问题消失。

#### B3　文案本地化与版本纠偏【新增】
- **目标**：文案与"六年级 + 广西北流 + 小升初"对齐。
- **具体做法**
  1. PC 侧边脚 `v60` → 改为读取 SW 版本号自动显示（避免再次滞后）。
  2. banner「人教版 · 1-6年级 · 让学习变得有趣」→「人教版 · 六年级 · 小升初冲刺」（按当前锁定年级动态生成）。
  3. 全站文案统一口径：北流/玉林、人教版、统编版、PEP。
- **预期效果**：家长感知到"这就是给我孩子用的"，版本不再滞后。

> 🚫 原「B4 PC 端窄屏软降级」**已取消**，不做改造（见第 2.4 节 P-12 与第 4 节否决清单）。以下内容原 B5 顺延为 B4。

#### B4　英语 PEP 模块（新旧双轨）【沿用，明确建库范围】
- **目标**：补齐三科中唯一脱节的学科。
- **具体做法**
  1. 按已确认版本建库：**旧版 PEP 三~五年级**（补欠账）+ **2024 修订版 PEP 六年级上/下**（同步新课），双轨并行，不只用一套。
  2. 题型：选词填空 / 单词拼写 / 英译中 / 句型转换；解除 `pc.js` 中「英语暂只支持单元练习」的考试限制。
  3. 现有音标拼读 1040 词条保留，作为"语音"子板块。
- **预期效果**：英语复习可执行，三科闭环完成。
- ✅ **U1 标题已确认**：**`Amazing places`**（2026-09-01 家长核对孩子课本目录页）。网络流传的 `Amazing landmarks` 为误传，建库以 `Amazing places` 为准，此待确认项已关闭。

### P2　增强项（锦上添花，按需排期）

#### C1　跨科薄弱雷达 + 错题筛选【沿用】
- 前置：错题记录补 `diff`/`unit` 字段；按难度/单元筛选定点重练。

#### C2　真题扩库补玉林/北流卷【沿用，提优先级说明】
- 纯数据工作，`injectRealIntoPaper`（`math.js:11323`）链路已通，零逻辑风险。在冲刺阶段（2027 年 4–5 月）前完成即可，但**素材采集需提前**。

#### C3　升学中心（时间线/目标/资讯）【修正：由 P0 下调至 P2】
- **修正理由**：既有《版块重构》列为 P0-C。重新评估——用户已确认走**公办对口/直升**，择校资讯价值低；且该版块不直接产生练习行为，需持续内容维护。对"助力升学"真正起作用的是 A1/A2/A3（每天练得下去 + 错题讲得透），升学信息属锦上添花。
- 建议形态：轻量静态卡片（可编辑），不接爬虫。

#### C4　移动端宽屏适配【新增】
- `≥768px` 时首页卡片两列、答题区加宽，服务家长在电脑端查看移动端页面。

---

## 4. 已否决 / 不可回归清单（避免重复投入）

| 项 | 状态 | 说明 |
|---|---|---|
| ❌ **「选择题型」筛选器** | v77 已完整删除 | 用户明确要求移除界面、交互、数据结构与逻辑。**不得以任何形式回归**（含"题型标签/题型过滤"等变体） |
| ❌ **「混合难度」第 0 档** | v77 已删 | 难度固定三档：1 基础 / 2 提高 / 3 拓展，默认基础。**不得加回混合档** |
| ❌ **「三遍验证」上线流程** | 2026-08-23 已简化 | 现行为「`node --check` 语法兜底 + 直接推送」，高风险/用户要求才跑 jsdom |
| ❌ **空题 toast 的其他文案** | 用户已确认 | 统一为「该单元暂无题目」，不要改 |
| ❌ **英语只建一套教材库** | 已确认双轨 | 2026 秋六年级启用 2024 修订版，须覆盖"旧版三~五 + 新版六"，只建一套必漏一半 |
| ❌ **PC 端复用移动端 index 页面** | 架构决定 | PC 有独立 `pc.html`/`pc.js`，不做合并 |
| 🚫 **PC 端窄屏软降级** | **2026-09-01 用户拍板取消** | 原拟将 `css/pc.css:84-99` 的 `@media(max-width:1023px)` 硬阻断改为软提示。**维持现状不做改造**——孩子主用手机 PWA，PC 工作台由家长在电脑上打开，窄屏场景极少，收益低于其他项。后续若出现强需求（如常用平板）再议，**勿主动重提** |

---

## 5. 分阶段实施顺序

> **通用铁律**：改 `css/js/HTML` 必须递增 `sw.js` 的 `CACHE` 版本（当前 `lyj-shell-v81`），否则用户端不拉新；推送后用 `curl https://lvqh2026.github.io/lyj-study/sw.js` 验证线上版本。

| 阶段 | 内容 | 涉及文件 | SW | 验收方式 |
|---|---|---|---|---|
| **S0（0.5 天）** ✅ **已完成** | 版本与文案纠偏：侧边脚读版本号、banner 改六年级、`viewport` 解除禁缩放 | `index.html`、`css/style.css`、`pc.html`、`js/pc.js`、`js/core.js`、`js/math.js`、`sw.js` | v78 | 已上线：verify_flow 全过 + 四端冒烟全过 + 真实浏览器 9/9 |

> **S0 实施备注（2026-09-01，commit `11e3c35`）**
> 1. **版本号方案改为动态读取**：原计划"侧边脚读版本号"落实为 `js/core.js` 新增 `readShellVersion()`，从 `sw.js` 文本解析 `const CACHE`（正则行首锚定，跳过注释中的同名文字），PC 页脚 `#pcFootVer` 动态写入。根治手写版本滞后（此前长期停留在 v60，滞后 17 版）。
> 2. **追加输入控件字号兜底**：解除 `user-scalable=no` 后，iOS 聚焦 <16px 输入框会强制放大页面，故在 `css/style.css` 为 `input,textarea,select` 兜底 `font-size:16px`。这是解除缩放的配套必要防护，原计划未列。
> 3. **追加一项卫生修复**：`js/math.js` 的 `renderHome()` 在 `math.js:12413` 被无条件调用，而 `pc.html` 无 `#gradeGrid`（年级网格属移动端），导致 PC 端每次加载抛 `Cannot set properties of null (setting 'innerHTML')`。已加空守卫返回。属既有缺陷，非 S0 引入。
| **S1（1–2 天）** ✅ **已完成** | **A1 导航统一 + 年级持久化** | `index.html`、`js/core.js`、`js/math.js`、`js/chinese.js`、`js/english.js`、`css/style.css`、`sw.js` | v79 | 已上线：verify_flow 全过 + 四端既有冒烟全过 + `_smoke_v79.js` 48/48 + 真实浏览器 21/21 |

> **S1 实施备注（2026-09-01，commit `d4fa8e0`）**
> 1. **全局页容器选型**：`#page-stats` / `#page-parent` 移出 `#mathRoot` 后统一挂在新建的 `#globalPages` 下，配合 `js/core.js` 新增的 `showGlobal(pageId)` 调度（显示全局页时隐藏全部三 root）。已核查 `css/style.css` 无 `#mathRoot` / `#chineseRoot` / `#englishRoot` 后代选择器，迁移零样式风险。
> 2. **追加「语文考试中心」**：原计划只要求"语文能进考试与统计"，但语文本来只有 `CN.beginExam` 逻辑、无移动端入口，导航给到「练习」tab 必然落空。故在 `js/chinese.js` 新增考试中心页（单元卷 / 上册期末 / 下册期末），导航「练习」在语文模块下直达该页。
> 3. **追加「英语进度入口回收」**：英语原 4 个 tab 收敛到全局 5 tab 后，「进度」无对应位置（`学=拼读 / 练=音标 / 错=错题本`）。已把该入口补回英语首页卡片区，功能不丢失。
> 4. **年级持久化键设计**：`localStorage['lyj_grade']` 存 JSON `{math:n, chinese:n}`，两科各自记住、互不干扰；缺省 6（六年级）。数学首页新增「当前年级」回显条，年级卡片加 `active` 高亮 + 「当前」角标，避免"已记住但看不出来"。
| **S2（1–2 天）** ✅ **已完成** | **A2 首页今日任务驾驶舱** | `index.html`、`js/math.js`、`css/style.css`、`sw.js`、`verify_flow.js` | v80 | 已上线：verify_flow 14 项 v80 断言全过 + `_smoke_v80.js` 52/52 + 真实浏览器 31/31 |

> **S2 实施备注（2026-09-01，commit `d64ac05`）**
> 1. **数据全部来自 localStorage，离线可用**：倒计时 / 任务完成判定 / 薄弱单元 / 本周概览都不依赖云端，错题与历史记录一直在 `math_practice_data` 里。
> 2. **三科任务完成判定分两种**：数学 / 语文用「当日 history 是否有记录」（自动），英语用「当日点击标记」（半自动，因为英语拼读是词汇表非选择题，没有判分记录）。
> 3. **命名冲突坑**：math.js 已存在 `findUnitByName(grade, name)`（3 处公式卡调用点），新增的「按单元名反查坐标」必须改名（用 `findUnitCoordByName`），否则函数声明提升会互相覆盖、把既有 3 处调用搞坏。冒烟测试里专门加了一条「既有 findUnitByName(grade,name) 未被覆盖」的断言防回归。
> 4. **`.btn{width:100%}` 跨样式坑**：通用按钮强制 100% 宽，本次加的「去做」小按钮若不显式 `width:auto` 会撑满整行挤压文字。`.dash-bar-fill` 加 `min-width:3%` 解决冲刺进度 0% 完全不可见的尴尬。`verify_flow.js` 顺手把 v77 / v79 SW 版本断言从「写死字符串」改为 `swVer() ≥ N` 解析版本下限校验——这俩断言本该随每次发版自动通过。
> 5. **埋点设计**：`lyj_task_click` 按天聚合任务点击数（留 30 天）。v80 不展示给家长看，但埋点已就位，下次做「家长后台」或统计页时可以接进来——家长关心「这驾驶舱是不是带动了练习」得有数据说话。
| **S3（2–3 天）** ✅ **已完成** | **A3 解析讲解回填** | `js/math.js`、`js/pc.js`、`css/style.css`、`css/pc.css`、`sw.js`、`verify_flow.js` | v81 | 已上线：**空壳率 89.2% → 0%**（全年级 644 道去重题 / 六年级 248 道，两层均为 0%）；verify_flow 21 项 v81 断言全过 + `_smoke_v81.js` 61/61 + 真实浏览器 23/23 |

> **S3 实施备注（2026-09-01，commit 待填）**
> 1. **先建覆盖率基线再动手**：写 `_steps_cover.js` 遍历 `KNOWLEDGE_BASE` 全单元 `gen()` 实测——**630 道去重题里 0% 自带 `explain`/`steps`**，全靠 `generateSteps` 现场生成，而旧兜底只吐一行「答案：x」，89.2% 的题等于没有讲解。没有这条基线就会误以为"补几道典型题"就够了。
> 2. **三层通用讲解引擎**（`buildSmartSteps`）：L1 句式规则（46 类题型：折扣/成数/比例尺/解比例/圆/圆柱圆锥/植树四类/角度六类/方向/鸽巢/工程/统计/最大公因数等）→ L2 分数关系反推 + 关键词列式 → L3 单元知识卡（`summary`/`fidx`/`method` 三素材）→ L4 兜底。实测分层：六年级真解析 58.9%、知识卡 27.8%、兜底 13.3%。
> 3. **「宁可不讲也不能讲错」是硬护栏**：L2 只在 `Math.abs(r - an) < 1e-6` 与答案精确一致、且题干确有运算信号时才列式，否则 `return null` 交给知识卡；折纸折角只在答案吻合 1/2 或相等时才输出。错题讲解讲错了比没有更伤孩子。
> 4. **三个真 BUG（冒烟抓到，均已修）**：① `verticalMulSteps` 位值错误，`242×5` 输出「100 + 10 + 1」（把数字当位值），改为「数字 × 位值」再拼；② 方向规则正则捕获组错位，`m[4]/m[5]/m[6]` 应为 `m[3]/m[4]/m[5]`，输出「东偏 undefinedundefined°」；③ 知识卡固定取 `summary[0]` 导致跑题（问"乘真分数积变小"却讲"分数乘整数"），新增 `pickRelevant()` 按题干汉字命中数打分挑最相关的一条。
> 5. **污染难度分级的坑**：兜底步骤会让 `steps.length >= 3` 几乎恒真，把 6:3:1 的梯度压平。已打 `q.stepsGeneric` 标记，两处难度判定（`difficultyScore`/`questionDifficulty`）显式排除，冒烟验证差值恰为 1。
> 6. **PC 端比预想更简陋**：原以为 `js/pc.js` 的 `renderWrongBank` 只是缺 steps，实际**连「你的答案 / 正确答案」都没有**，只有题干 + 错次。已一并补答案行与讲解块，与移动端对齐。
> 7. **老错题自动回填**：入库时存的空壳 steps（形如 `['答案：200元']`）在渲染时判定「<2 步 或 单行答案」即重新生成，历史错题无需清库即可享受新解析。
| **S4（1–2 天）** | **B1 图标统一 + B2 样式回收 + B3 文案本地化** | 全站 `html`/`css`/`js` | v82 | 全站 emoji 清零（★与确认✅除外）；行内样式 <10 处；按钮 ≥48px |
| **S5（3–5 天）** | **B4 英语 PEP 双轨模块** | `js/english.js`、`js/data.js`、`index.html`、`js/pc.js` | v83 | 六上六下单元可练可选难度；英语可进考试中心 |
| **S6（按需）** | C1 薄弱雷达 / C2 本地真题 / C3 升学中心 / C4 宽屏适配 | — | v84+ | 逐项单测 |

**依赖关系**：S3 与 S1 可并行；**S5 依赖 S1 完成**（英语需先有统一导航与考试页）；S6 中 C1 依赖错题补 `diff`/`unit` 字段。
**已剔除**：原「S5 PC 窄屏软降级」已取消（见第 4 节），不占排期、不占 SW 版本。

---

## 6. 风险与注意事项

1. **S1 改动面最大**：语文/英语新增页面会触碰 `#chineseRoot`/`#englishRoot` 结构与 `CN`/英语模块内部路由，须保留既有两段式批改流程（成绩页 → 确认 → 答案解析）与 `recordHistory`/`syncAfterQuiz` 调用。
2. **S4 全站替换图标**：属高风险批量改动，建议**先用 `node --check` 逐文件过语法，再上 jsdom 冒烟**，不要只做语法兜底。
3. **英语建库前必须确认新版六上 U1 标题**（课本目录页为准），否则 topic 建错需返工。
4. **每次上线后用户端需清一次 PWA 缓存 / 强制刷新**才能拉到新资产。
5. 数学题库改动须回归既有管线：`_cap.js` → `_audit_cross.js` → `_test_all_units.js`。

---

## 附：本版相对既有文档的差异一览

| 条目 | 既有结论 | 本版 | 类型 |
|---|---|---|---|
| 三科导航统一 | P0-A | P0-A1（扩至英语 tab 重构 + 年级持久化） | 沿用+扩范围 |
| 首页驾驶舱 | P0-B | P0-A2（强化倒计时/任务/薄弱） | 沿用+强化 |
| 解析讲解 | P0-1 | P0-A3 | 沿用 |
| 升学中心 | P0-C | **P2-C3** | **修正（下调）** |
| PC 窄屏降级 | P2-G / 曾建议 P0 | **取消不做** | **取消（用户 2026-09-01 拍板；维持硬阻断现状）** |
| 英语 PEP 模块 | P1-3 | P1-B4（明确新旧双轨范围；原 B5 编号顺延） | 沿用+明确 |
| 薄弱雷达 / 真题扩库 | P1-E / P2-5 | P2-C1 / P2-C2 | 沿用 |
| **emoji 治理** | 无 | **P1-B1** | **新增** |
| **内联样式与触控尺寸** | 无 | **P1-B2** | **新增** |
| **文案本地化与版本纠偏** | 无 | **P1-B3** | **新增** |
| **viewport 禁缩放** | 无 | S0 | **新增** |
| **移动端宽屏适配** | 无 | P2-C4 | **新增** |
| **年级不持久化** | 未识别 | P0-A1 | **新增** |
